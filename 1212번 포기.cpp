#include <iostream>
#include <string>

using namespace std;
char itoa(int n)
{
	if (n == 0) return '0';
	if (n == 1) return '1';
}

int main()
{
	int num;
	char* str;
	char* tok;
	str = new char[3333334];
	cin >> str;
	tok = copy();
	while (1) 
	{
		
	}


/*	while (1)
	{
		int buf = num % 2;
		char st;
		st = itoa(buf);
		num = num / 2;
		str.push_back(st);
		if (num == 0) break;
	}
	int len = str.length();
	for (int i = len-1; i != 0; i--)
	{
		cout << str[i];
	}
	cout << str[0] << endl;*/
}